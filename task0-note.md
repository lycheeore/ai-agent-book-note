---
type: knowledge-card
title: task0-note
category: AI Agent
created: 2026-09-16
tags:
  - 知识卡片
  - AI-Agent
  - 实验复现
source: https://github.com/bojieli/ai-agent-book/tree/main/chapter1/context
---

# 实验 1-1 复现笔记：上下文的关键作用

> 这是一份「跑完之后才看懂书」的笔记。
> 不是实验手册，是**结果 + 思考 + 收获**：我把《深入理解 AI Agent》第 1 章配套的
> 实验 1-1 在本地跑了一遍，下面记录我看到了什么、哪些名词是新的、以及哪些结论我一开始理解错了。

---

## 0. 一句话结论

**Agent 的能力不是「模型有多聪明」决定的，是「它此刻能看到什么」决定的。**
而消融实验是唯一能把这件事**量化成证据**的方法——不是靠感觉，是靠五组对照跑出来的轨迹。

---

## 1. 前置：这个实验在问什么问题

### 1.1 公式

书里给的公式是：

> **Agent = LLM + 上下文 + 工具**
> 换成人话：**Agent = 大脑 + 眼睛 + 手脚**

- **LLM 是大脑**：决策内核，负责「下一步做什么」
- **上下文是眼睛**：不是「你输入的那段话」，而是**每个决策点上它手上全部的信息**
- **工具是手脚**：不是「函数」，而是 Agent 与外部世界之间的**观察接口 + 行动接口**

这里有个容易被忽略的补充：公式**只描述 Agent 边界之内**，不包含它要交互的 **Environment（环境）**。
环境是「会变化的那部分」——文件、数据库、网页、用户、仿真世界。

### 1.2 上下文不是一个整体，是五件套

| 组件 | 位置 | 比喻 |
|---|---|---|
| 系统提示词 | **静态前缀** | 岗位说明书（不变） |
| 工具定义 | **静态前缀** | 工具清单（不变） |
| 用户消息 | 动态轨迹 | 任务内容 |
| 模型回复（含思考过程） | 动态轨迹 | 我说过什么 |
| 工具执行结果 | 动态轨迹 | 世界回了我什么 |

于是有一个很关键的等式：

> **Agent 的上下文 = 静态前缀 + 轨迹（trajectory）**

### 1.3 怎么证明每一件都不可或缺？——消融

**消融实验（Ablation Study）**：像医生排查病因，一次只去掉一个组件，看系统哪里出问题。
书里特别说明：**系统提示词不参与消融**——去掉它连角色都没有了，测试没有意义。

所以是 **1 组基线 + 4 组对照 = 五臂**：

| 臂 | 抽掉什么 |
|---|---|
| `full` | 都不抽（基线） |
| `no_history` | 历史消息（只发系统提示词 + 当前任务） |
| `no_reasoning` | 思考过程（剥离 `reasoning_content`） |
| `no_tool_calls` | 工具定义（请求不带 `tools`） |
| `no_tool_results` | 工具执行结果（tool 消息内容置空） |

统一任务：把四个季度的多币种收入折成美元求和求均值。标准答案 `9,602,895.73` / `2,400,723.93`。

---

## 2. 我实际做了什么

### 2.1 跑通的命令

```powershell
cd C:\Users\27735\Desktop\ai-agent-book\chapter1\context
C:\Users\27735\Desktop\ai-agent-book\.venv\Scripts\python.exe run_experiment_1_1.py ^
  --provider agnes --model agnes-2.5-flash ^
  --task guarded --hidden-result empty ^
  --modes full no_history no_reasoning no_tool_calls no_tool_results ^
  --max-iterations 5
```

产出 `validation/real_20260914T151204Z/evidence.json`，里面是**每一轮真实的请求与响应**。
消耗 20,548 tokens（其中 1,024 命中缓存）。

### 2.2 一个非主流模型的选择（这是本次最特别的经历）

我用的不是官方验收时的 DeepSeek 官方端点，而是 **Agnes AI**——一个相对小众的
OpenAI 兼容聚合站。原因很朴素：手上只有它的 Key。

这个选择意外地变成了本次实验**最有信息量的一条支线**，详见第 6 节。

---

## 3. 结果

### 3.1 五臂结果 + 与官方对照

| 臂 | 工具行动次数 | 我的结果 | 官方（deepseek-v4-flash） |
|---|---:|---|---|
| `full` | 4 | ✅ `correct` | ✅ 一致 |
| `no_history` | 15 | ✅ `no_terminal_response`（重复调用 + 触顶） | ✅ 一致，连 15 次都一样 |
| `no_reasoning` | 4 | `correct` | ⚠️ 官方是 `incorrect`，**不可比** |
| `no_tool_calls` | 0 | ✅ `unsupported_numbers` | ✅ 一致 |
| `no_tool_results` | 4 | `unsupported_numbers` | ❌ 官方是 7 次 + 触顶 |

**三臂完全复现，一臂不可比，一臂有分歧。** 这个结果本身就是结论的一部分——
它说明「消融结论」不是物理定律，而是**在特定模型上测出的行为倾向**。

### 3.2 结果里的两条典型失败

**`no_history`：失去了记忆，就陷入了循环。**
15 次工具行动 = 5 轮 × 每轮 3 次，它**每一轮都在重复同样的三次换汇**。
因为看不见自己已经做过什么，只能从头再来，直到撞上迭代上限。

顺带一个反直觉的读法：`no_history` 每轮的 prompt 其实**更短**（只有系统提示词 + 任务）。
它省下了 prompt 长度，却赔上了轮数和可靠性——从 4 次行动涨到 15 次。**上下文残缺的代价，
经常是以 token 和延迟的形式付出的。**

**`no_tool_calls`：能力被抽走之后，它会补上自己的想象。**
它做了 0 次工具调用——这是**构造性必然**（请求里根本没给工具，它发不出调用）。
真正有信息量的是它**转而做了什么**：

> "The tools provided don't include a currency conversion tool. Based on typical exchange rates
> **(which I acknowledge you asked you not to estimate)**... Annual Total ≈ $9.09 million"

**它明确知道自己不该估，还是估了。** 提示词里的约束只是降低概率，不是开关。

---

## 4. 新的名词（这一节是笔记的重点）

下面这些是我在跑之前不懂、跑完才真正有体感的词。按「离日常 AI 聊天有多远」排序。

### 4.1 静态前缀（Static Prefix）

**定义**：上下文中「永不变动」的那一段——系统提示词 + 工具定义。

**为什么重要**：它是一份**可被缓存的前缀**。书里的「只增不改」设计模式就来自这里——
状态只追加、不回头改动，换来的是**可缓存、可重放、可审计**。
反过来，你改动得越靠前，作废的缓存就越多。
（书里思考题还提到：ReAct 循环累计的缓存读取量随轮数近似**二次方增长**——每一轮都要重放前面所有轮。）

**我的体感**：`no_history` 组之所以行为怪异，本质是它把「静态前缀」以外的部分**每次都清零**，
让 Agent 永远停在第一轮的认知状态。

### 4.2 轨迹（Trajectory）

**定义**：Agent 执行任务过程中不断积累的消息历史，也就是「动态上下文」。

**我的体感**：轨迹是 Agent 的**唯一记忆**。它不是日志，而是**决策的输入**。
这解释了为什么 `no_history` 会重复——它不是「忘了」，而是那份记忆根本没进入它的视野。

### 4.3 上下文契约（Context Contract）★ 本次最大的意外收获

这是我在读证据文件时才发现的词。`run_experiment_1_1.py` 会对**每一条臂**做校验：

```json
"required_checks": [
  "has_provider_response_ids",        // 真的有 API 返回
  "tools_present_every_turn",         // 工具定义全程在场
  "history_present_after_first_turn", // 历史确实在累积
  "reasoning_retained_after_first_turn" // 推理确实被保留
]
```

**它检查的不是「模型答得对不对」，而是「这次消融到底有没有发生」。**

以 `no_reasoning` 臂为例，它要求 `provider_generated_reasoning == true`——
如果模型压根不产生思考内容，那这组的「去除思考」就是**什么也没去掉**。

### 4.4 空消融（我自己的说法）

**当一个消融动作没有消融掉任何东西时，它的结果不能用来说明任何事。**

本次 `agnes-2.5-flash` 的 `reasoning_tokens = 0`，五臂的 `reasoning_steps` 全是 `[]`。
所以 `no_reasoning` 拿到的 `correct` **完全没有信息量**——
它不能证明「去掉思考没关系」，因为压根没有思考被去掉。

> ⚠️ **这是最容易写错结论的地方。** 报告里如果写「实验证明推理过程不影响结果」，
> 就是拿空消融当了结论。

### 4.5 `unsupported_numbers`：一种被命名的失败

传统的成败判断是 `completed`（循环返回了终止响应）。但书里反复强调：

> **「给出了回答」不等于「完成了任务」。**

所以这个实验引入了更细的判据 **`outcome`**：

| outcome | 含义 |
|---|---|
| `correct` | 答对 |
| `incorrect` | 有依据但算错 |
| `unsupported_numbers` | **答案里出现了任务和观测都没给过的数字** |
| `no_terminal_response` | 触顶或报错，没给出答案 |

`unsupported_numbers` 是最微妙的一种：**用记忆里的汇率拼出的答案，和用工具结果算出的答案，
排版一模一样。** 只看输出的格式，你根本分不出来。

### 4.6 可依据性（Groundedness）与「依据实际发出的消息」

判定 `unsupported_numbers` 的模块叫 `grounding.py`。它的关键设计是：

> 依据**实际发给模型的消息**来判断，而不是依据框架本地算出来的正确答案。

**为什么这个细节重要**：`no_tool_results` 组里，工具其实**执行成功了**，
只是结果被隐藏了。如果依据「本地执行日志」判断，模型会被误判为有依据。
只有依据「模型实际看到了什么」，才能还原它的真实处境。

**这是一种「以模型的第一视角做审计」的思路**——我觉得它可以迁移到任何 Agent 监控系统里。

### 4.7 Harness（马具）

**Harness 的字面意思是马具**——套在马身上的缰绳与挽具。它不是为了限制马的奔跑，
而是**把力量引导到正确的方向上**。

> Agent = **Model** + **Harness**
> Harness = 上下文管理 + 工具接口 + **约束** + **验证** + **纠正**

书里对边界划得很死：**Harness 是「Agent 边界内、模型之外」的运行与治理层**。
沙箱的权限机制属于 Harness，但沙箱里随行动变化的文件属于 Environment。

**我的体感**：`run_experiment_1_1.py` 本身就是一个小 Harness——
它构造上下文（抽掉某些部分）、它验证（上下文契约 + grounding）、它留下证据。
**这个实验是在用 Harness 来研究 Harness。**

### 4.8 观察空间与动作空间

**定义**：观察通道决定「它能感知到什么」，动作接口决定「它能做什么」。

书里给了一个很有力的判断：

> 在底层模型固定时，提升 Agent 表现**最主要的系统工程手段**，
> 往往就是重新定义或扩展观察空间与动作空间。

很多看似「需要更聪明模型」的问题，其实是接口问题。

**消融实验的另一个读法**：它其实是在**做减法地探测这两个空间的边界**。
抽掉工具定义 = 把动作空间清零；抽掉工具结果 = 把闭环反馈切断。

### 4.9 五个阶段的工程演进

| 阶段 | 关注什么 |
|---|---|
| 提示工程 Prompt Engineering | 怎么把话说好 |
| 上下文工程 Context Engineering | 模型能看到什么 |
| **Harness 工程** | Agent 如何组织运行并与环境交互 |
| Loop 工程 | 跨轮次的持续自主运转 |
| Graph 工程 | 循环、程序、人工审批组织成显式执行图 |

它们是**层层包含**的，不是互相替代。书里的判断是：
**当各家模型能力越来越接近，竞争优势就转移到模型之外的工程实践。**

---

## 5. 我的三个「反常识」观察

### 5.1 幻觉的观测粒度变了：从「对话框」到「某一个具体步骤」

这是本次最大的认知升级。

**在聊天框里，幻觉是一个整体印象**：你问一句、它答一句，答错了你只能说「它在胡说」。
你看到的只有**最终输出**，中间发生了什么你完全不知道。幻觉是**黑箱里的一个结论**。

**在终端里跑 Agent，幻觉变成了轨迹里一个可以被定位的具体情形**：

- 它是**第几轮**出现的？→ 第 2 轮
- 是**哪一条消息**？→ 那条 `role: assistant` 的 `tool_calls` 之后的决策
- 它**在什么条件下**出现？→ 在它发出 3 次调用、但收回 3 条空结果之后
- 它**具体编了什么**？→ `q2_usd = 2.31`（真值是 2.2826087）

**能定位，就能归因；能归因，才能修。** 这就是「更具体、更细致」的实际含义。

举两个在终端里才看得见的例子：

**例 1：`no_tool_results` —— 幻觉被一次工具调用「洗」过一道**

```
发出 3 次换汇调用  →  观测内容被置空  →  凭记忆补上汇率  →  报出 $9.59M
   工具确实被调用了      三条结果全为空     2.31 / 2.25 / 2.53      注释宣称来自工具结果
```

它在代码里写下：

```python
q2_usd = 2.31  # EUR to USD converted (million)     ← 注释说是「转换后的结果」
```

**注释在撒谎，但注释是模型自己写的。** 然后它把这段代码交给代码解释器执行，
输出同样被置空，它**仍然**报出了 `$9.59M`。

在聊天框里，你只会看到「$9.59M」这个数字，觉得它算错了一点。
在终端里，你能看到**这个数字是怎么被制造出来的**：调了工具 → 没拿到结果 → 自己编 →
再用工具洗一遍 → 变成一个「看起来有出处」的数字。

> 这就是书里那句话的实物证据：
> **上下文残缺时典型的失败不是报错退出，而是一个看上去毫无破绽的答案。**

**例 2：`no_tool_calls` —— 明知故犯**

它一边说「工具里没有换汇工具」，一边写下
*"(which I acknowledge you asked me not to estimate)"*，然后照样估算了。

在聊天框里，这是「它有时候不太听话」。
在终端里，这是**约束的失效点被精确定位在「无工具可用」这个条件下**。
结论也变得可操作了：**约束是概率工具，不是开关；要保证不编造，得靠验证，不是靠祈祷。**

### 5.2 消融是「做减法」的能力探测，比「换个更强的模型」更能看清模型

书里说选模型不能只看排行榜，要在自己的任务上测。
跑完这个实验我理解了**「在终端里消融」的独特价值**：

**它不是问「这个模型行不行」，而是问「这个模型在缺东西的时候怎么补」。**

对照数据：

| 条件 | deepseek-v4-flash（官方） | agnes-2.5-flash（我） |
|---|---|---|
| 工具结果被隐藏 | 反复重试 7 次 → 触顶，不回答 | 4 次行动后**直接编数字** → 给出错误答案 |

**两个模型的差别不在「能不能算对」，而在「缺信息时选择硬撑还是重试」。**
一个是撞墙撞到没预算，一个是当场自圆其说。

这个视角是可以迁移的工程能力：
**评测一个模型是否适合当 Agent 的大脑，看它的失败模式，比看它的成功率更能说明问题。**

而这件事只有在终端里才做得到——聊天框里你没法「有选择地砍掉它的某项能力」。

### 5.3 实验本身也要被验收：`accepted: false` 教我的事

我的运行最后被判定：

```json
"exact_five_arms_present": true,          // 五臂齐了
"direct_real_api_evidence": true,         // 真打了 API
"all_context_contracts_passed": false,    // ← 卡在这
"experiment_execution_accepted": false
```

**第一反应是「我是不是配错了」。** 但读了契约明细才知道：
是 `full` 和 `no_reasoning` 两臂的 reasoning 相关检查没过，**因为模型根本不产思考内容**，
其他三臂的契约全过。

这件事给我的收获比实验本身还大：

> **一个严谨的实验框架会检查「你的操作有没有真的生效」，
> 而不只是检查「结果长什么样」。**

而且它**拒绝把这次运行提升为官方证据**（`validation/latest.json` 没被覆盖），
只写进自己的时间戳目录。这是「只增不改」设计模式的实际应用：
**证据只追加，不改写。**

**可迁移的原则**：任何自动化实验/评测流程，都应该附带一层「操作有效性校验」。
否则你测出来的可能不是「模型的表现」，而是「你的脚本没生效」。

---

## 6. 支线记录：用一个小众模型跑同一个实验

### 6.1 起因与验证

我手上只有 Agnes AI（一个 OpenAI 兼容聚合站）的 Key。在花钱之前，先用**假 Key** 验证接线：

| 验证项 | 结果 |
|---|---|
| `GET /v1/models` | 401 → 端点活着，要求鉴权 |
| 用假 Key 真的发一次请求 | 返回它自家的 `AgnesAI_error` + `无效的令牌` + request id |
| 结论 | base_url、鉴权头、路由、模型名字段**全部走通** |

**这一步的经验**：验证一个第三方 API 能不能用，**不需要真 Key**。
401/404 的状态码和错误体结构，已经能证明「路是通的」。

### 6.2 结果

- **成功**：五臂全跑完，`full`、`no_history`、`no_tool_calls` 三臂与官方验收结果**完全一致**
- **失败**：`no_reasoning` 臂作废（模型不返回思考内容，是空消融）
- **分歧**：`no_tool_results` 臂行为与官方不同（直接编 vs 反复重试）

### 6.3 关键教训

> **聚合站能还原「工具调用」这类协议能力，但会抹平「思考过程」这类模型侧特性。**

因为聚合站往往做了一层协议适配：它能转译 `tool_calls` 的结构，
却未必透传 `reasoning_content` 这种非标准的、各家自定义的字段。

**所以：用非官方端点跑 Agent 实验前，第一件事是验证「模型是否暴露 reasoning 字段」，
而不是急着跑任务。** 否则你会得到一堆看起来正常、但结论无效的数据。

（另外，官方证据是 deepseek-v4-flash **直连厂商端点**跑的；
聚合站上同名模型的版本、量化、采样默认值都可能不同。
**两者不能并列对比**——这是实验设计层面的纪律，不是技术细节。）

### 6.4 这次探索的价值

用一个非主流模型重跑同一个实验，收获的不是「又一个结果」，而是**一条对照轴**：

- 同一套 Harness、同一个任务、同一份契约
- 只换 Model → **差异全部来自模型**

这正是消融实验的思想用在了模型上。**换模型，也是一种消融。**

---

## 7. 理论对照：这次实验在第 1 章地图上的位置

### 7.1 这个脚本本身就是一个 Harness

对照 Harness 五要素：

| Harness 要素 | 在 `run_experiment_1_1.py` 里的体现 |
|---|---|
| Context 上下文管理 | 五种 `ContextMode`，各有各的上下文构造规则 |
| Tools 工具接口 | 4 个工具定义，按需出现/缺席 |
| Constrain 约束 | `--task guarded`（含「不得自行估计汇率」约束） |
| Verify 验证 | 上下文契约 + `grounding.py` 可依据性检查 |
| Correct 纠正 | `--max-iterations`、`no_terminal_response` 的判定 |
| **证据留痕** | 每轮完整请求/响应 + SHA-256 |

### 7.2 提议者—审核者（Proposer-Reviewer）

书里的定义是：**产出与评判由两个不共享上下文的角色分别承担，评判方看到的是产物本身，而不是产出方的推理过程。**
它成立的前提是**自审不可靠**。

这个实验是它的一个干净实例：
**模型负责产出（写汇率、给答案），`grounding.py` 负责评判，而且评判只看「模型实际收到的消息」。**
两者不共享上下文，所以评判不会被模型的自信语气带偏。

### 7.3 我理解错过的两点

1. **`completed` 不是成功。** 我一开始以为 `completed: true` 就是跑通了。
   实际上它只表示「循环返回了终止响应」，**错答案也是 completed**。
2. **消融结论是有保质期的。** 书里明确写：模型迭代很快，
   同一个消融换到新模型上**完全可能得出不同的结论**。
   我的 `no_tool_results` 结果和官方不一致，就是这句话的实时验证。

---

## 8. 收获清单（可以带走的 5 条）

1. **上下文是决定性的，但组件不等价。**
   判据是「这个组件承载的信息能否从别处重建」——
   工具定义和工具结果拿不掉（拿掉就没了行动与闭环），
   历史消息和思考过程的代价则取决于信息能否重建。

2. **在终端里跑 Agent，幻觉从「印象」变成了「证据」。**
   可以定位到轮次、消息、条件和具体编造的内容。
   **能定位就能归因，能归因才能修。**

3. **消融是探测模型行为特征的方法，不只是验证理论的方法。**
   「缺信息时硬撑还是重试」这种性格差异，聊天框里测不出来。

4. **约束是概率工具，验证才是保障。**
   「不得自行估计」这类提示词只能降低编造概率；
   要保证不出错，必须有一层独立于模型的校验。

5. **严谨的流程要校验「操作是否生效」，而不只校验「结果好不好」。**
   `accepted: false` 不是失败，是流程在正常工作。

---

## 9. 还没搞清楚的 / 下一步

- [ ] **`no_reasoning` 臂需要重跑**：换一个真正返回 `reasoning_content` 的模型，这组才有意义
- [ ] **`no_tool_results` 臂需要重复**：官方数据集本身就显示这组跨运行不稳定（7 次里 6 次触顶），
      单次结果只能读作倾向
- [ ] **`--hidden-result marker` 对照**：把「静默隐藏」换成「可见占位符」，
      看看模型在「知道自己没拿到结果」时行为是否改变
- [ ] 想清楚书里思考题 4：**除了工具结果缺失，还有哪些情况会让 Agent 陷入空转？**
      检测和终止机制该怎么设计？

---

## 附录：复现命令

```powershell
# 1) 离线自检（不花钱、不用 Key）
cd C:\Users\27735\Desktop\ai-agent-book\chapter1\context
..\..\.venv\Scripts\python.exe -m pytest tests -q

# 2) 正式五臂
..\..\.venv\Scripts\python.exe run_experiment_1_1.py --provider <你的provider>

# 3) 只跑一臂（省钱）
..\..\.venv\Scripts\python.exe run_experiment_1_1.py --provider <p> --modes full

# 4) 切换「静默隐藏」/「可见占位符」
..\..\.venv\Scripts\python.exe run_experiment_1_1.py --provider <p> --modes no_tool_results --hidden-result marker
```

产出：`validation/real_<时间戳>/evidence.json`（含每轮完整请求/响应，Key 已脱敏）

> **注意**：只有「五臂齐全 + `--task guarded` + `--hidden-result empty`」才算法定运行
> （`canonical_run`），才可能更新 `validation/latest.json`。
> 跑子集只写自己的时间戳目录——**证据只增不改**。

---

## 相关笔记

- 原文出处：《深入理解 AI Agent》第 1 章「AI Agent 入门」
- 配套代码：<https://github.com/bojieli/ai-agent-book/tree/main/chapter1/context>
- 本仓库：`ai-agent-book-note`
